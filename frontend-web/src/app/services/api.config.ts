// URL base del backend Spring Boot (BakendPasantias), que a su vez persiste
// todo en la base de datos Postgres que corre en Docker (docker-compose.yml).
export const API_BASE_URL = 'http://localhost:8080/api';
